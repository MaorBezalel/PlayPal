<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.StartApp" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@color/splash_screen</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_icon_min</item>
        <item name="postSplashScreenTheme">@style/Theme.PlayPal</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

</resources>